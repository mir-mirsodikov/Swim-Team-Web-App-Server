import 'mocha';
import chai from 'chai';

const expect = chai.expect;

describe('Basic testing with Mocha/Chai', () => {
  it('Are Mocha and Chai correctly set up?', () => {
    expect(true).to.be.true;
  });
});
